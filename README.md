Identify your directory address.
Copy it.
Open the Command Prompt on your machine.
Type cd (change directory) at the beginning of the line and paste the ZIP’s path.
Run the command npm install in the next terminal line.
Open the project in VS Code using code ..
Finally, start the connection by running ionic serve in the terminal and wait for it to open a localhost page in your browser.
